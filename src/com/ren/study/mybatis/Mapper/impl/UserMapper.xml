<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ren.study.mybatis.Mapper.UserMapper">
	<resultMap type="user" id="userMapper">
		<result property="id" column="id"/>
		<result property="userName" column="user_name"/>
		<result property="password" column="password"/>
		<result property="sexEnum" column="sex"
			 jdbcType="VARCHAR" javaType="sexEnum"/>
			 <!--如果 直接typeHandler="com.ren.study.mybatis.typeHandler.SexEnumTypeHandler" 就不用再配置文件中配置typeHandler了 -->
		<result property="mobile" column="mobile"/>
		<result property="tel" column="tel"/>
		<result property="email" column="email"/>
		<result property="note" column="note"/>
	</resultMap>
	
	<select id="getUser" resultMap="userMapper" parameterType="long">
		select * from t_user where id=#{id}
	</select>
	
</mapper>